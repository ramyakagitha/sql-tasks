create database pbks;
use pbks;
CREATE TABLE departments (
    dept_id INT PRIMARY KEY,
    dept_name VARCHAR(50) NOT NULL,
    location VARCHAR(50)
);
INSERT INTO departments VALUES
(1, 'IT', 'Hyderabad'),
(2, 'HR', 'Delhi'),
(3, 'Finance', 'Mumbai'),
(4, 'Sales', 'Hyderabad'),
(5, 'Marketing', 'Chennai'); 

CREATE TABLE employees (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50) NOT NULL,
    dept_id INT,
    salary INT,
    FOREIGN KEY (dept_id) REFERENCES departments(dept_id)
);
INSERT INTO employees VALUES
(101, 'Ram', 1, 60000),
(102, 'Sita', 2, 40000),
(103, 'John', 1, 75000),
(104, 'Amit', 3, 50000),
(105, 'Sara', 4, 65000),
(106, 'David', 1, 45000),
(107, 'Kiran', NULL, 30000);  

CREATE TABLE customers (
    cust_id INT PRIMARY KEY,
    cust_name VARCHAR(50) NOT NULL
);
INSERT INTO customers VALUES
(1, 'Arjun'),
(2, 'Meena'),
(3, 'Ravi'),
(4, 'Priya'),
(5, 'Kavya');  

CREATE TABLE orders (
    order_id INT PRIMARY KEY,
    cust_id INT,
    amount INT,
    FOREIGN KEY (cust_id) REFERENCES customers(cust_id)
);
INSERT INTO orders VALUES
(201, 1, 6000),
(202, 2, 3000),
(203, 1, 7000),
(204, 3, 2000),
(205, 1, 8000),
(206, 2, 5500),
(207, 1, 4000),
(208, NULL, 9000);  

CREATE TABLE payments (
    payment_id INT PRIMARY KEY,
    order_id INT,
    amount INT,
    FOREIGN KEY (order_id) REFERENCES orders(order_id)
);
INSERT INTO payments VALUES
(1, 201, 6000),
(2, 202, 3000),
(3, 203, 7000),
(4, NULL, 5000);  

CREATE TABLE products (
    prod_id INT PRIMARY KEY,
    prod_name VARCHAR(50) NOT NULL
);
INSERT INTO products VALUES
(1, 'Laptop'),
(2, 'Mobile'),
(3, 'Tablet'),
(4, 'Headphones'),
(5, 'Camera');  

CREATE TABLE order_details (
    order_id INT,
    prod_id INT,
    quantity INT,
    FOREIGN KEY (order_id) REFERENCES orders(order_id),
    FOREIGN KEY (prod_id) REFERENCES products(prod_id)
);
INSERT INTO order_details VALUES
(201, 1, 2),
(202, 2, 1),
(203, 1, 1),
(204, 3, 3),
(205, 2, 2),
(206, 4, 5),
(207, 1, 1);

CREATE TABLE students (
    student_id INT PRIMARY KEY,
    student_name VARCHAR(50) NOT NULL
);
INSERT INTO students VALUES
(1, 'Anu'),
(2, 'Bala'),
(3, 'Charan'),
(4, 'Divya'),
(5, 'Esha');  

CREATE TABLE courses (
    course_id INT PRIMARY KEY,
    course_name VARCHAR(50) NOT NULL
);
INSERT INTO courses VALUES
(101, 'Python'),
(102, 'Java'),
(103, 'SQL'),
(104, 'C++');  

CREATE TABLE enrollments (
    student_id INT,
    course_id INT,
    FOREIGN KEY (student_id) REFERENCES students(student_id),
    FOREIGN KEY (course_id) REFERENCES courses(course_id)
);
INSERT INTO enrollments VALUES
(1, 101),
(2, 102),
(3, 101),
(4, 103),
(1, 103);

CREATE TABLE customer_contacts (
    cust_id INT,
    phone VARCHAR(15),
    FOREIGN KEY (cust_id) REFERENCES customers(cust_id)
);

INSERT INTO customer_contacts VALUES
(1, '9876543210'),
(2, '9123456780'),
(3, '9988776655');

CREATE TABLE categories (
    category_id INT PRIMARY KEY,
    category_name VARCHAR(50)
);

INSERT INTO categories VALUES
(1, 'Electronics'),
(2, 'Accessories');

CREATE TABLE product_categories (
    prod_id INT,
    category_id INT,
    FOREIGN KEY (prod_id) REFERENCES products(prod_id),
    FOREIGN KEY (category_id) REFERENCES categories(category_id)
);

INSERT INTO product_categories VALUES
(1, 1),
(2, 1),
(3, 1),
(4, 2);

CREATE TABLE attendance (
    student_id INT,
    attendance_date DATE,
    status VARCHAR(10),
    FOREIGN KEY (student_id) REFERENCES students(student_id)
);

INSERT INTO attendance VALUES
(1, '2026-05-01', 'Present'),
(2, '2026-05-01', 'Absent'),
(3, '2026-05-01', 'Present'),
(4, '2026-05-01', 'Present');

CREATE TABLE suppliers (
    supplier_id INT PRIMARY KEY,
    supplier_name VARCHAR(50)
);

INSERT INTO suppliers VALUES
(1, 'ABC Suppliers'),
(2, 'Tech Distributors');

CREATE TABLE product_suppliers (
    prod_id INT,
    supplier_id INT,
    FOREIGN KEY (prod_id) REFERENCES products(prod_id),
    FOREIGN KEY (supplier_id) REFERENCES suppliers(supplier_id)
);

INSERT INTO product_suppliers VALUES
(1, 1),
(2, 1),
(3, 2),
(4, 2);

CREATE TABLE projects (
    project_id INT PRIMARY KEY,
    project_name VARCHAR(50)
);

INSERT INTO projects VALUES
(1, 'AI Project'),
(2, 'Banking App'),
(3, 'E-Commerce');

CREATE TABLE employee_projects (
    emp_id INT,
    project_id INT,
    FOREIGN KEY (emp_id) REFERENCES employees(emp_id),
    FOREIGN KEY (project_id) REFERENCES projects(project_id)
);

INSERT INTO employee_projects VALUES
(101, 1),
(101, 2),
(102, 3),
(103, 1),
(105, 2);

CREATE TABLE salary_records (
    emp_id INT,
    salary_amount INT,
    salary_date DATE,
    FOREIGN KEY (emp_id) REFERENCES employees(emp_id)
);

INSERT INTO salary_records VALUES
(101, 60000, '2026-01-01'),
(101, 65000, '2026-04-01'),
(102, 40000, '2026-03-01'),
(103, 75000, '2026-02-01');

CREATE TABLE users (
    user_id INT PRIMARY KEY,
    user_name VARCHAR(50)
);

INSERT INTO users VALUES
(1, 'Anil'),
(2, 'Bhavana'),
(3, 'Chandu'),
(4, 'Deepika');

CREATE TABLE login_records (
    login_id INT PRIMARY KEY,
    user_id INT,
    login_time DATETIME,
    FOREIGN KEY (user_id) REFERENCES users(user_id)
);

INSERT INTO login_records VALUES
(1, 1, '2026-05-01 09:00:00'),
(2, 2, '2026-05-01 10:00:00'),
(3, 1, '2026-05-02 08:30:00');

INNER JOIN 
use pbks;

select e.emp_name,d.dept_name from employees e inner join departments d on e.dept_id=d.dept_id;

select o.order_id,c.cust_name,o.amount from orders o inner join customers c on o.cust_id=c.cust_id;

select s.student_name,c.course_name from students s inner join enrollments e on s.student_id=e.student_id
inner join courses c on e.course_id=c.course_id;

select p.prod_name,od.quantity from products p inner join order_details od on p.prod_id=od.prod_id;

select emp_name,salary from employees;

select e.emp_name,d.dept_name from employees e inner join departments d on e.dept_id=d.dept_id
where d.dept_name='IT';

select c.cust_name,o.amount from customers c inner join orders o on c.cust_id=o.cust_id
where o.amount>5000;

select c.cust_name,count(o.order_id) from customers c inner join orders o on c.cust_id=o.cust_id
group by c.cust_name;

select emp_name,salary from employees where salary>50000;

select s.student_name,c.course_name from students s inner join enrollments e on s.student_id=e.student_id
inner join courses c on e.course_id=c.course_id where c.course_name='Python';

select e.emp_name,d.dept_name,d.location from employees e inner join departments d
on e.dept_id=d.dept_id where d.location='Hyderabad';

select d.dept_name,e.emp_name,e.salary from employees e inner join departments d on e.dept_id=d.dept_id
where e.salary=(select max(salary) from employees where dept_id=e.dept_id);

select c.cust_name,count(o.order_id) from customers c inner join orders o on c.cust_id=o.cust_id
group by c.cust_name having count(o.order_id)>3;

select p.prod_name,sum(od.quantity)as total_quantity from products p inner join order_details od on
p.prod_id=od.prod_id group by p.prod_name;

select emp_name,salary from employees where salary>(select avg(salary) from employees);

LEFT JOIN

select e.emp_name,d.dept_name from employees e left join departments d on e.dept_id=d.dept_id;

select o.order_id,p.payment_id from orders o left join payments p on o.order_id=p.order_id;

select c.cust_name,cc.phone from customers c left join customer_contacts cc on c.cust_id=cc.cust_id;

select p.prod_name,c.category_name  from products p left join product_categories pc on p.prod_id=pc.prod_id
left join categories c on pc.category_id=c.category_id;

select s.student_name,a.attendance_date,a.status from students s left join attendance a on s.student_id=a.student_id;


select e.emp_name,d.dept_name from employees e left join departments d on e.dept_id=d.dept_id
where d.dept_name is null;

select o.order_id,o.amount,p.payment_id from orders o left join payments p on o.order_id=p.order_id
where p.payment_id is null;

select c.cust_name,o.order_id from customers c left join orders o on c.cust_id=o.cust_id where  order_id is null;

select p.prod_name from products p left join product_suppliers ps on p.prod_id=ps.prod_id 
where ps.supplier_id is null;

select e.emp_name,count(ep.project_id) as total_projects from employees e left join employee_projects ep 
on e.emp_id=ep.emp_id group by e.emp_name;

select distinct c.cust_name from customers c left join orders o on c.cust_id=o.cust_id 
left join payments p on o.order_id=p.order_id where p.payment_id is null and o.order_id is not null;

select e.emp_name,sr.salary_amount,sr.salary_date from employees e left join salary_records sr on
e.emp_id=sr.emp_id and sr.salary_date=(select max(salary_date) from salary_records where emp_id=e.emp_id);

select p.prod_name,sum(od.quantity) as total_quantity from products p left join order_details od on p.prod_id=od.prod_id
group by p.prod_name;

select u.user_name from users u left join login_records lr on u.user_id=lr.user_id where lr.login_id is null;


select d.dept_name,count(e.emp_id) as total_employees from departments d left join employees e on d.dept_id=e.dept_id
group by d.dept_name;

RIGHT JOIN
create database srh;
use srh;
CREATE TABLE orders (
    order_id INT PRIMARY KEY,
    order_amount INT
);

INSERT INTO orders VALUES
(101, 5000),
(102, 7000),
(103, 3000);

CREATE TABLE payments (
    payment_id INT PRIMARY KEY,
    order_id INT,
    payment_amount INT,
    FOREIGN KEY (order_id) REFERENCES orders(order_id)
);

INSERT INTO payments VALUES
(1, 101, 5000),
(2, 102, 7000),
(3, NULL, 4000);

CREATE TABLE customers (
    cust_id INT PRIMARY KEY,
    cust_name VARCHAR(50)
);

INSERT INTO customers VALUES
(1, 'Arjun'),
(2, 'Meena'),
(3, 'Ravi');

CREATE TABLE transactions_table (
    trans_id INT PRIMARY KEY,
    cust_id INT,
    amount INT,
    FOREIGN KEY (cust_id) REFERENCES customers(cust_id)
);

INSERT INTO transactions_table VALUES
(201, 1, 6000),
(202, 2, 3000),
(203, NULL, 4500);

CREATE TABLE invoices (
    invoice_id INT PRIMARY KEY,
    order_id INT,
    invoice_amount INT,
    FOREIGN KEY (order_id) REFERENCES orders(order_id)
);

INSERT INTO invoices VALUES
(301, 101, 5000),
(302, NULL, 2500);

CREATE TABLE employees (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50)
);

INSERT INTO employees VALUES
(1, 'Ram'),
(2, 'Sita');

CREATE TABLE attendance (
    attendance_id INT PRIMARY KEY,
    emp_id INT,
    status VARCHAR(20),
    FOREIGN KEY (emp_id) REFERENCES employees(emp_id)
);

INSERT INTO attendance VALUES
(1, 1, 'Present'),
(2, NULL, 'Absent');

CREATE TABLE shipments (
    shipment_id INT PRIMARY KEY,
    order_id INT,
    shipment_status VARCHAR(30),
    FOREIGN KEY (order_id) REFERENCES orders(order_id)
);

INSERT INTO shipments VALUES
(1, 101, 'Shipped'),
(2, NULL, 'Pending');

CREATE TABLE refunds (
    refund_id INT PRIMARY KEY,
    order_id INT,
    refund_amount INT,
    FOREIGN KEY (order_id) REFERENCES orders(order_id)
);

INSERT INTO refunds VALUES
(1, 101, 1000),
(2, NULL, 500);

CREATE TABLE systems (
    system_id INT PRIMARY KEY,
    system_name VARCHAR(50)
);

INSERT INTO systems VALUES
(1, 'Server A'),
(2, 'Server B');

CREATE TABLE logs (
    log_id INT PRIMARY KEY,
    system_id INT,
    log_message VARCHAR(100),
    FOREIGN KEY (system_id) REFERENCES systems(system_id)
);

INSERT INTO logs VALUES
(1, 1, 'Login Success'),
(2, NULL, 'Unknown Access');

CREATE TABLE users (
    user_id INT PRIMARY KEY,
    user_name VARCHAR(50)
);

INSERT INTO users VALUES
(1, 'Anil'),
(2, 'Bhavana');

CREATE TABLE bookings (
    booking_id INT PRIMARY KEY,
    user_id INT,
    booking_date DATE,
    FOREIGN KEY (user_id) REFERENCES users(user_id)
);

INSERT INTO bookings VALUES
(1, 1, '2026-05-07'),
(2, NULL, '2026-05-08');

CREATE TABLE audit_logs (
    audit_id INT PRIMARY KEY,
    system_id INT,
    activity VARCHAR(100),
    FOREIGN KEY (system_id) REFERENCES systems(system_id)
);

INSERT INTO audit_logs VALUES
(1, 1, 'Data Updated'),
(2, NULL, 'Unknown Modification');

select o.order_id,p.payment_id,p.payment_amount from orders o right join payments p on o.order_id=p.order_id;

select c.cust_name,t.trans_id,t.amount from customers c right join transactions_table t on
c.cust_id=t.cust_id;

select o.order_id,i.invoice_id,i.invoice_amount from orders o right join invoices i on
o.order_id=i.order_id;

select e.emp_name,a.attendance_id,a.status from employees e right join attendance a on
e.emp_id=a.emp_id;

select o.order_id,s.shipment_id,s.shipment_status from orders o right join shipments s on
o.order_id=s.order_id;

select p.payment_id,p.payment_amount,o.order_id,o.order_amount from orders o right join payments p 
on o.order_id=p.order_id;

select c.cust_name,t.trans_id,t.amount from customers c right join transactions_table t on
c.cust_id=t.cust_id;

select r.refund_id,r.refund_amount,o.order_amount from orders o right join refunds r on
o.order_id=r.order_id;

select l.log_id,l.log_message,s.system_name from systems s right join logs l on
s.system_id=l.system_id;

select b.booking_id,b.booking_date,u.user_name from users u right join bookings b on
u.user_id=b.user_id;

select p.payment_id,p.payment_amount,o.order_id,o.order_amount from orders o right join payments p on
o.order_id=p.order_id where o.order_id is null; 

select t.trans_id,t.amount,c.cust_name from customers c right join transactions_table t on
c.cust_id=t.cust_id where c.cust_id is null;

select a.audit_id,ifnull(s.system_name,'System Missing') as system_status,a.activity from systems s 
right join audit_logs a on s.system_id=a.system_id;

select i.invoice_id,ifnull(o.order_id,'No order') as order_status,i.invoice_amount from orders o right join invoices i on
o.order_id=i.order_id;

select s.shipment_id,o.order_id from orders o right join shipments s on o.order_id=s.order_id;